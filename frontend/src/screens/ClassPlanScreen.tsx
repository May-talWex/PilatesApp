/**
 * @deprecated This file exists only to preserve any imports that reference the
 * flat module path. The actual implementation lives in ClassPlanScreen/index.tsx.
 *
 * TypeScript prefers this .tsx file over ClassPlanScreen/index.tsx when resolving
 * bare imports of 'ClassPlanScreen', so we re-export from the folder explicitly.
 */
export { default } from './ClassPlanScreen/index';
