/**
 * @deprecated This file exists only to preserve any imports that reference the
 * flat module path. The actual implementation lives in InjurySelectionScreen/index.tsx.
 *
 * TypeScript prefers this .tsx file over InjurySelectionScreen/index.tsx when resolving
 * bare imports of 'InjurySelectionScreen', so we re-export from the folder explicitly.
 */
export { default } from './InjurySelectionScreen/index';
