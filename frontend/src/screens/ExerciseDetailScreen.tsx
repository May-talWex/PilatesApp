/**
 * @deprecated This file exists only to preserve any imports that reference the
 * flat module path. The actual implementation lives in ExerciseDetailScreen/index.tsx.
 *
 * TypeScript prefers this .tsx file over ExerciseDetailScreen/index.tsx when resolving
 * bare imports of 'ExerciseDetailScreen', so we re-export from the folder explicitly.
 */
export { default } from './ExerciseDetailScreen/index';
